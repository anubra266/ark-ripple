import { mergeProps } from 'zag-ripple';
import { trackSplit, track } from 'ripple';
import { useColorPickerContext } from './use-color-picker-context';
import { usePresenceContext } from '../presence';
import { ark } from '../factory';

export interface ColorPickerPositionerBaseProps {
  children?: any;
  [key: string]: any;
}
export type ColorPickerPositionerProps = ColorPickerPositionerBaseProps;

export component ColorPickerPositioner(props: ColorPickerPositionerProps) {
  const [children, localProps] = trackSplit(props, ['children']);
  const colorPicker = useColorPickerContext();
  const presence = usePresenceContext();

  let mergedProps = track(() => mergeProps(@colorPicker.getPositionerProps(), @localProps));

  if (!@presence.unmounted) {
    <ark.div {...@mergedProps}>
      <@children />
    </ark.div>
  }
}
