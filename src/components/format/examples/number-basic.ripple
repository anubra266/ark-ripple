import { Format } from 'ark-ripple/format';
import styles from 'styles/format.module.css';

export component NumberBasic() {
	<span class={styles.Value}>
		<Format.Number value={1450.45} />
	</span>
}
