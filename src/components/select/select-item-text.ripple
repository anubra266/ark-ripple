import { mergeProps } from 'zag-ripple';
import { trackSplit, track } from 'ripple';
import { useSelectContext } from './use-select-context';
import { useSelectItemPropsContext } from './use-select-item-props-context';
import { ark } from '../factory';

export interface SelectItemTextBaseProps {
	children?: any;
	[key: string]: any;
}
export type SelectItemTextProps = SelectItemTextBaseProps;

export component SelectItemText(props: SelectItemTextProps) {
	const [children, localProps] = trackSplit(props, ['children']);
	const select = useSelectContext();
	const itemProps = useSelectItemPropsContext();

	let mergedProps = track(() => mergeProps(@select.getItemTextProps(@itemProps), @localProps));

	<ark.span {...@mergedProps}>
		<@children />
	</ark.span>
}
