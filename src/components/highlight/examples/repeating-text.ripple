import { Highlight } from 'ark-ripple/highlight';
import styles from 'styles/highlight.module.css';

export component RepeatingText() {
  <p class={styles.Text}>
    <Highlight
      class={styles.Mark}
      query="@ark-ui.rip"
      text="Contact us at support@ark-ui.rip or sales@ark-ui.rip for assistance."
      matchAll
    />
  </p>
}
