import { mergeProps } from 'zag-ripple';
import { trackSplit, track } from 'ripple';
import { useDrawerContext } from './use-drawer-context';
import { ark } from '../factory';

export interface DrawerCloseTriggerBaseProps {
  children?: any;
  [key: string]: any;
}
export type DrawerCloseTriggerProps = DrawerCloseTriggerBaseProps;

export component DrawerCloseTrigger(props: DrawerCloseTriggerProps) {
  const [children, localProps] = trackSplit(props, ['children']);
  const drawer = useDrawerContext();

  let mergedProps = track(() => mergeProps(@drawer.getCloseTriggerProps(), @localProps));

  <ark.button {...@mergedProps}>
    <@children />
  </ark.button>
}
