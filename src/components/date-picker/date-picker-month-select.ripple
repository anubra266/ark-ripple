import { mergeProps } from 'zag-ripple';
import { trackSplit, track } from 'ripple';
import { useDatePickerContext } from './use-date-picker-context';
import { ark } from '../factory';
import type { HTMLProps, MaybeTracked, PolymorphicProps } from '../../types';

export interface DatePickerMonthSelectBaseProps extends PolymorphicProps<'select'> {}
export interface DatePickerMonthSelectProps extends HTMLProps<
  'select'
>, DatePickerMonthSelectBaseProps {}

export component DatePickerMonthSelect(props: MaybeTracked<DatePickerMonthSelectProps>) {
  const [localProps] = trackSplit(props, []);
  const datePicker = useDatePickerContext();

  let mergedProps = track(() => mergeProps(@datePicker.getMonthSelectProps(), @localProps));

  <ark.select {...@mergedProps}>
    for (const month of @datePicker.getMonths()) {
      <option value={month.value}>{month.label}</option>
    }
  </ark.select>
}
