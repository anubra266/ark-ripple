import { mergeProps } from 'zag-ripple';
import { trackSplit, track } from 'ripple';
import type { PresetTriggerProps } from '@zag-js/date-picker';
import { useDatePickerContext } from './use-date-picker-context';
import { ark } from '../factory';
import type { HTMLProps, MaybeTracked, PolymorphicProps } from '../../types';

export interface DatePickerPresetTriggerBaseProps extends PolymorphicProps<'button'>, PresetTriggerProps {}
export interface DatePickerPresetTriggerProps extends HTMLProps<'button'>, DatePickerPresetTriggerBaseProps {}

export component DatePickerPresetTrigger(props: MaybeTracked<DatePickerPresetTriggerProps>) {
  const [children, value, localProps] = trackSplit(props, ['children', 'value']);
  const datePicker = useDatePickerContext();

  const presetTriggerProps = track(() => ({ value: @value }));
  let mergedProps = track(
    () => mergeProps(@datePicker.getPresetTriggerProps(@presetTriggerProps as any), @localProps),
  );

  <ark.button {...@mergedProps}>
    <@children />
  </ark.button>
}
