import { mergeProps } from 'zag-ripple';
import { trackSplit, track } from 'ripple';
import { useDatePickerContext } from './use-date-picker-context';
import { useDatePickerTablePropsContext } from './use-date-picker-table-props-context';
import { ark } from '../factory';

export interface DatePickerTableRowBaseProps {
  children?: any;
  [key: string]: any;
}
export type DatePickerTableRowProps = DatePickerTableRowBaseProps;

export component DatePickerTableRow(props: DatePickerTableRowProps) {
  const [children, localProps] = trackSplit(props, ['children']);
  const datePicker = useDatePickerContext();
  const tableProps = useDatePickerTablePropsContext();

  let mergedProps = track(() => mergeProps(@datePicker.getTableRowProps(@tableProps), @localProps));

  <ark.tr {...@mergedProps}>
    <@children />
  </ark.tr>
}
