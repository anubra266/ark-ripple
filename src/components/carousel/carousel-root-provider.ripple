import { mergeProps } from 'zag-ripple';
import { trackSplit, track } from 'ripple';
import { CarouselApiContext } from './use-carousel-context';
import type { UseCarouselReturn } from './use-carousel.ripple';
import { ark } from '../factory';
import type { HTMLProps, MaybeTracked, PolymorphicProps } from '../../types';

export interface CarouselRootProviderBaseProps extends PolymorphicProps<'div'> {
  value: UseCarouselReturn;
}
export interface CarouselRootProviderProps extends HTMLProps<
  'div'
>, CarouselRootProviderBaseProps {}

export component CarouselRootProvider(props: MaybeTracked<CarouselRootProviderProps>) {
  const [carousel, children, rest] = trackSplit(props, ['value', 'children']);

  CarouselApiContext.set(carousel);

  let mergedProps = track(() => mergeProps(@carousel.getRootProps(), @rest));

  <ark.div {...@mergedProps}>
    <@children />
  </ark.div>
}
