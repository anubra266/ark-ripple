import { mergeProps } from 'zag-ripple';
import { trackSplit, track } from 'ripple';
import { useComboboxContext } from './use-combobox-context';
import { useComboboxItemPropsContext } from './use-combobox-item-props-context';
import { ark } from '../factory';

export interface ComboboxItemTextBaseProps {
  children?: any;
  [key: string]: any;
}
export type ComboboxItemTextProps = ComboboxItemTextBaseProps;

export component ComboboxItemText(props: ComboboxItemTextProps) {
  const [children, localProps] = trackSplit(props, ['children']);
  const combobox = useComboboxContext();
  const itemProps = useComboboxItemPropsContext();

  let mergedProps = track(() => mergeProps(@combobox.getItemTextProps(@itemProps), @localProps));

  <ark.span {...@mergedProps}>
    <@children />
  </ark.span>
}
