import { EnvironmentProvider } from '@ark-ui/ripple/environment'
import { Frame } from '@ark-ui/ripple/frame'
import { Usage } from './usage.ripple'

export component Basic() {
  <Frame title="IFrame Context" style={{ width: '100%', height: '100%' }}>
    <EnvironmentProvider>
      <Usage />
    </EnvironmentProvider>
  </Frame>
}
